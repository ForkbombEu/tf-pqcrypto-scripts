# With this script we print the pubkeys contained into ./contracts/keys/pubkeys.keys.json

Rule unknown ignore
Given I send path 'pubkeys-file' and read file content and output into 'pubkeys'
Given I have a 'base64 dictionary' named 'pubkeys'
Then print the 'pubkeys'